import React from 'react';
import InterviewChat from './InterviewChat';

export default {
  title: 'Organisms/Interview Chat',
  component: InterviewChat,
};

export const interviewChat = (args) => <InterviewChat {...args} />;
