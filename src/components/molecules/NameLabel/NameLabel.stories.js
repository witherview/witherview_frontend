import React from 'react';
import NameLabel from './NameLabel';

export default {
  title: 'Molecules/Name Label',
  component: NameLabel,
};

export const nameLabel = (args) => <NameLabel {...args} />;
