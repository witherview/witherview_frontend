import React from 'react';
import ChatInput from './ChatInput';

export default {
  title: 'Molecules/Chat Input',
  component: ChatInput,
};

export const chatInput = (args) => <ChatInput {...args} />;
