import React from 'react';
import ProfileEdit from './ProfileEdit';

export default {
  title: 'Molecules/Profile Edit',
  component: ProfileEdit,
};

export const profileEdit = (args) => <ProfileEdit {...args} />;
